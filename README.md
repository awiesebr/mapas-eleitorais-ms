# Mapas eleitorais de Mato Grosso do Sul

Páginas estáticas com mapas de votação por município. O conteúdo é criptografado (AES-256-GCM, chave derivada do PIN com PBKDF2-SHA256) e só abre com o PIN de acesso.

Para publicar: em Settings → Pages, escolha a branch `main` e a pasta `/ (root)`.
